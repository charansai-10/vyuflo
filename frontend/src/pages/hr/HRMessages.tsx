// src/pages/hr/HRMessages.tsx
//
// HR Secure Messaging page.
// The SecureMessaging component is fully data-driven — it calls
// GET /messages/conversations which returns only the current user's threads.
// No role-specific logic needed; this is a pure route wrapper.

export { default } from "../employee/SecureMessaging";